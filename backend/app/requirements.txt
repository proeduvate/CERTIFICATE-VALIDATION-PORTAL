fastapi
uvicorn[standard]
sqlalchemy
# PostgreSQL driver (psycopg 3). SQLAlchemy URL: postgresql+psycopg://...
psycopg[binary]
# Schema migrations. The app no longer calls create_all at import — run
# `alembic upgrade head` instead.
alembic
python-dotenv
python-jose[cryptography]
passlib[bcrypt]
# passlib 1.7.4 probes the backend with a >72-byte password. bcrypt >= 4.1
# raises instead of truncating, which makes every hash_password() call fail
# with "password cannot be longer than 72 bytes". Pin until passlib ships a fix.
bcrypt==4.0.1
python-multipart
pydantic-settings
email-validator
# Imported by routes/intern.py (xlsx export) and utils/pdf_generator.py.
# Both were missing, so the app could not even be imported.
openpyxl
reportlab
qrcode[pil]
psycopg2
