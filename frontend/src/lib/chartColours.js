/**
 * Series colours: a ramp of the brand hue rather than a rainbow.
 *
 * Segments stay distinguishable without introducing decorative colours that
 * carry no meaning.
 */
export const SERIES_COLOURS = [
    'var(--brand-500)',
    'var(--brand-300)',
    'var(--brand-700)',
    'var(--brand-200)',
    'var(--brand-800)',
];
